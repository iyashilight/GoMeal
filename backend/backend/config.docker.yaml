server:
    port: 8080

database:
    user: root
    password: ""          # 通过环境变量 DB_PASSWORD 设置
    host: mysql
    port: 3306
    dbname: order_system

redis:
    addr: redis:6379
    password: ""          # 通过环境变量 REDIS_PASSWORD 设置
    db: 0

jwt:
    secret_key: ""        # 通过环境变量 JWT_SECRET 设置
    expire_hours: 72
